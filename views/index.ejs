<%- include('./partials/header.ejs') %>
<%- include('./partials/homeNavbar.ejs') %>

<body>
    <div class="container">
        <% article.forEach(article =>{ %>
        <div class="card">
            <div class="card-header">
                <h3><%= article.title  %></h3>
            </div>
            <div class="card-body">
                <a href="<%= article.slug %>" class="btn bg-success">read article</a>
            </div>
        </div>
        <%})%>
    </div>
</body>
<%- include('./partials/footer.ejs') %>